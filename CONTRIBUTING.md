# 贡献说明

感谢关注 CC Notice。当前项目仍在快速迭代。

欢迎通过 Issue 反馈问题、建议和硬件测试结果。

## 开发流程

1. 安装依赖：`npm ci`
2. 前端验证：`npm run build`
3. 单元测试：`npm run test`
4. Rust 代码格式化检查：按改动范围执行 `cargo fmt --check`

固件构建不属于 app 的默认开发构建流程。除非变更明确涉及 `src-tauri/assets/firmware` 或固件烧录逻辑，否则不要要求贡献者安装 Arduino CLI、Pico SDK、STM32CubeProgrammer 等固件工具链。

## 代码要求

- 保持现有模块边界，避免无关重构。
- 修改用户可见行为时补充测试。
- 涉及发布、路径、设备接入、固件内置资产的改动，需要同步更新 `docs/`。
- 不提交本地日志、证书、环境变量文件、构建产物和签名资产。

## 反馈问题

Issue 建议包含：

- 操作系统和版本
- 软件版本或提交
- 相关硬件型号
- 复现步骤
- 期望结果和实际结果

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "custom-face-image-pixelizer",
  "description": "Permissions for the custom face image pixelizer tool window.",
  "windows": ["custom-face-image-pixelizer"],
  "permissions": ["core:default", "dialog:allow-open"]
}

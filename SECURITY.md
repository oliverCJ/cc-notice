# 安全说明

## 支持范围

当前项目处于早期测试阶段，安全问题优先覆盖：

- 本地 Hook 服务认证
- Hook 配置写入
- 本地配置和 token 文件权限
- 桌面应用打包和发布资产
- 固件烧录入口的外部命令调用

## 数据和隐私边界

CC Notice 默认在本机处理 Hook 事件。本地 Hook 服务绑定到 `127.0.0.1`，事件提交需要本机生成的 Hook token。

调试页面会展示最近接收的 Hook payload 摘要；启用 Hook Debug 后，还可能展示 AI 工具提交的原始 payload。payload 可能包含 prompt、工具响应、工作目录、会话标识等敏感信息。公开截图、日志或问题反馈前，请先确认其中没有敏感内容。

Webhook 输出会按用户配置把请求头和请求体发送到外部 URL。若模板中使用 `{{prompt}}`、`{{tool_response}}`、`{{last_assistant_message}}`、`{{pwd}}` 等变量，这些内容会随 Webhook 外发。只应把 Webhook 指向可信服务。

## 发布资产边界

仓库可以提交已开放给用户的固件产物，但不提交本机生成的 `src-tauri/assets/tools/cc-notice-relay`。该 relay 是平台相关二进制，用户或 CI 构建软件时应由脚本在本机生成并打包。

## 报告安全问题

请不要在公开 Issue 中直接披露可利用细节。可以先创建不含细节的 Issue，说明需要私下沟通安全问题；维护者确认后再提供复现步骤和影响范围。

报告中建议包含：

- 受影响版本或提交
- 操作系统
- 复现步骤
- 预期影响
- 可行的缓解建议

## 发布签名状态

免费发布包可能未进行平台代码签名。macOS 未签名、未公证包可能触发 Gatekeeper 拦截；这不代表包一定被篡改，但会影响普通用户安装体验。正式公开稳定版发布前应补充平台签名和公证流程。
